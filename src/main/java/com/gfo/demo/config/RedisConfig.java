package com.gfo.demo.config;

import org.springframework.context.annotation.Configuration;

/**
 * Redis configuration - disabled for demo purposes
 * Uncomment and configure when Redis is available
 */
@Configuration
public class RedisConfig {
    // Empty configuration for demo - Redis functionality disabled
    // When Redis is available, implement proper Redis cache configuration
}